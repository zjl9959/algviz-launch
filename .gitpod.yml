tasks:
  - name: setup-env
    before: sudo apt-get install -y graphviz
    init: python -m pip install --upgrade pip
    command: pip install algviz && exit
vscode:
  extensions:
    - ms-python.Python
    - ms-toolsai.Jupyter